apiVersion: v1
kind: ConfigMap
metadata:
  name: pixelclock-config
data:
  # Prod talks to the real device — prefix must match what AWTRIX is configured with.
  awtrix-prefix: "awtrix"
  tick-seconds: "1"
